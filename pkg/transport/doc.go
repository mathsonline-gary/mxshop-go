// Package transport abstracts the transport layer, providing a unified interface for different protocols.
// It provides the implementation of  the communication protocol transport layer of gRPC and HTTP by default.
package transport
